# Domain docs

This project uses a single-context layout. 

Agents should consult the root `CONTEXT.md` (if it exists) and the Architecture Decision Records in `docs/adr/` for domain knowledge and technical constraints.
