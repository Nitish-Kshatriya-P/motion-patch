# Issue tracker

Issues are tracked as local markdown files under `.scratch/<feature-slug>/issues/`.

When interacting with tickets, agents should read and update these local markdown files directly.
